{{- if .Values.frontend.nginxConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webapp.fullname" . }}-nginx
  labels:
    {{- include "webapp.labels" . | nindent 4 }}
data:
  default.conf: |
    {{- .Values.frontend.nginxConfig.config | nindent 4 }}
{{- end }}
